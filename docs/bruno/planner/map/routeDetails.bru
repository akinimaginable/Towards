meta {
  name: Route Details
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/map/route-details
  body: none
  auth: none
}

params:query {
  routeIdx: 0
  ~language: fr
}

headers {
  Accept: application/json
}

docs {
  **GET** `/api/v1/map/route-details`
  
  Returns the full data for a single route, including all stops and
  polyline segments.
  
  operationId: routeDetails
}
